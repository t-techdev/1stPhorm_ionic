import { ActiveRouteDirective } from './active-route.directive';

describe('ActiveRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveRouteDirective();
    expect(directive).toBeTruthy();
  });
});
